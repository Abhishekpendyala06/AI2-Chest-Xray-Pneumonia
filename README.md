# AI2-Chest-Xray-Pneumonia
This project uses a CNN with MobileNetV2 transfer learning to detect Pneumonia from chest X-ray images. The pretrained model extracts features, and a custom CNN head performs classification, achieving about 87% accuracy on test data.
